-

    name: This is my first play.
    hosts: sqlserver1
    tasks:
      -

        name: "Create a file on sqlserver1"
        script: testScript.sh abhay creates=/tmp/test.txt
          # If /tmp/test.txt exist at nodes, then testScript.sh will not execute.  
