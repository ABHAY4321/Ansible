-

    name: This is my first play.
    hosts: webserver1
    vars:
      servicename: apache2
    tasks:
      -

        name: "Starting service using variable on webserver1"
        service: name={{ servicename }} state=started
